# Dependencies
node_modules/
frontend/node_modules/
backend/node_modules/

# Build output
frontend/dist/

# Environment files — never commit secrets
backend/.env
frontend/.env
# .env.production is safe to commit (no secrets, just the backend URL)
# but if you put secrets in it, add it here too

# Explanation files
AUTH_EXPLANATION.txt
REVIEWS_EXPLANATION.txt
CHANGES_EXPLANATION.txt
